403:
  description: Access denied!
  content:
    application/json:
      schema:
        $ref: "../../definitions/common.yaml#/exception_error"
      examples:
        when access denied:
          value:
            messages:  Unauthorized!
        when can not update status:
          value:
            messages: Sorry! You can't change status
